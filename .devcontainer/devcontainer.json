{
  "name": "Root and Custom DNS Codespace",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "runArgs": [
    "--dns", "76.76.19.19",     // Primary Custom DNS (IPv4)
    "--dns", "76.223.122.150",  // Secondary Custom DNS (IPv4)
    "--dns", "2602:fcbc::ad",   // Primary Custom DNS (IPv6)
    "--dns", "2602:fcbc:2::ad"  // Secondary Custom DNS (IPv6)
  ],
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  "postCreateCommand": "echo 'Root and custom DNS servers configured'"
}
